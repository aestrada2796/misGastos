server {
    listen 80;
    listen [::]:80;
    server_name minio.my_expenses.ae s3.my_expenses.ae;

    return 301 https://$host$request_uri; # Redirección a HTTPS
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name minio.my_expenses.ae;

    http2 on;
    ssl_early_data on;

    # Certificados SSL
    ssl_certificate     /etc/nginx/certs/_wildcard.my_expenses.ae.pem;
    ssl_certificate_key /etc/nginx/certs/_wildcard.my_expenses.ae-key.pem;

    ssl_protocols TLSv1.2 TLSv1.3;  # Asegúrate de usar protocolos seguros
    ssl_ciphers 'TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers on;

    # Configuración para MinIO (sin ningún prefijo)
    location / {
        if ($content_length != $http_content_length) {
            return 400 "Content-Length incorrecto";
        }

        proxy_pass http://my_expenses_s3:9001;  # Redirige a MinIO en el puerto 9001
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        error_page 502 503 504 /error.html;
    }

    location = /error.html {
        root /usr/share/nginx/html;
        internal;
    }

    # Logs
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Compresión Gzip
    gzip on;
    gzip_types
        text/plain
        text/css
        application/json
        application/javascript
        text/xml
        application/xml
        application/xml+rss
        text/javascript
        image/svg+xml
        application/vnd.ms-fontobject
        application/x-font-ttf
        font/opentype;
    gzip_min_length 1024;
    gzip_vary on;
    gzip_disable "msie6";

    # Optimización para seguridad
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    add_header Permissions-Policy "accelerometer=(), autoplay=(), camera=(), geolocation=(), gyroscope=(), magnetometer=(), microphone=(), payment=(), usb=()" always;

    # Límites de carga de archivos
    client_max_body_size 100M;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name s3.my_expenses.ae;

    http2 on;
    ssl_early_data on;

    # Certificados SSL
    ssl_certificate     /etc/nginx/certs/_wildcard.my_expenses.ae.pem;
    ssl_certificate_key /etc/nginx/certs/_wildcard.my_expenses.ae-key.pem;

    ssl_protocols TLSv1.2 TLSv1.3;  # Asegúrate de usar protocolos seguros
    ssl_ciphers 'TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers on;

    # Configuración para MinIO (sin ningún prefijo)
    location / {
        if ($content_length != $http_content_length) {
            return 400 "Content-Length incorrecto";
        }

        proxy_pass http://my_expenses_s3:9000;  # Redirige a MinIO en el puerto 9001
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        error_page 502 503 504 /error.html;
    }

    location = /error.html {
        root /usr/share/nginx/html;
        internal;
    }

    # Logs
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Compresión Gzip
    gzip on;
    gzip_types
        text/plain
        text/css
        application/json
        application/javascript
        text/xml
        application/xml
        application/xml+rss
        text/javascript
        image/svg+xml
        application/vnd.ms-fontobject
        application/x-font-ttf
        font/opentype;
    gzip_min_length 1024;
    gzip_vary on;
    gzip_disable "msie6";

    # Optimización para seguridad
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    add_header Permissions-Policy "accelerometer=(), autoplay=(), camera=(), geolocation=(), gyroscope=(), magnetometer=(), microphone=(), payment=(), usb=()" always;

    # Límites de carga de archivos
    client_max_body_size 100M;
}
